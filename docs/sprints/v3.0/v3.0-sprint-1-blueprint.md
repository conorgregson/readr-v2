# Sprint 1 Blueprint — Deployment Audit & Environment Hardening

Readr v3.0

**Objective:** Audit the current Vercel + Render + Neon deployment and remove environment and configuration fragility. This sprint establishes a clear production baseline for the rest of v3.0 hardening work.

---

# Sprint 1 Goal

- Production deployment paths are clearly understood
- Environment variables are standardized
- Localhost assumptions are removed
- Frontend, backend, and database connectivity is validated end-to-end

---

# Scope

## Deployment Audit

- Document the current Vercel frontend deployment
- Document the current Render backend deployment
- Document Neon’s role in the production architecture
- Verify request flow between frontend, backend, and database

---

## Environment Review

- Audit all environment variables used by client and server
- Standardize `.env.example` files
- Confirm production and local variable naming is consistent
- Remove stale, duplicate, or unused environment keys

---

## Production Config Hardening

- Remove any remaining localhost fallbacks in production paths
- Verify frontend API base URL resolution
- Verify backend CORS configuration against the deployed frontend origin
- Verify database connection handling in production

---

# Execution Checklist

## 1) Audit deployed Vercel + Render + Neon architecture

- [x] Confirm the production frontend host and URL
- [x] Confirm the production backend host and URL
- [x] Confirm the backend health endpoint
- [x] Confirm Neon is the active production database provider
- [x] Map the production request flow:
  - browser -> Vercel frontend -> Render backend -> Neon database
- [x] Identify legacy or unused deployment assumptions
- [x] Confirm that several Neon/Postgres variables in Vercel are integration-managed rather than manually created

## 2) Verify production connectivity and Neon reliability baseline

- [x] Load the deployed frontend successfully after Sprint 1 merge to `main`
- [x] Inspect the browser Network tab for live API requests after Sprint 1 merge to `main`
- [x] Confirm no production requests target localhost after Sprint 1 merge to `main`
- [x] Hit the backend health endpoint directly after Sprint 1 merge to `main`
- [x] Test at least one database-backed production route after Sprint 1 merge to `main`
- [x] Note any cold starts, flaky startup, timeout behavior, or deployment pain points during post-merge verification
  - No significant deployment issues observed during hosted Sprint 1 verification.

## 3) Standardize environment variables across client, server, and deployment

### Client audit

- [x] Search for `import.meta.env`
- [x] Search for `VITE_`
- [x] Search for `localhost`
- [x] Search for hard-coded API URLs
- [x] List all client environment variables actually used
- [x] Compare actual usage against local client env configuration
- [x] Confirm `VITE_API_BASE_URL` is the active client runtime variable
- [x] Confirm `VITE_API_BASE` is stale and should not be used
- [x] Correct local client environment naming and value shape
- [x] Create `client/.env.example`

### Server audit

- [x] Search for `process.env`
- [x] Search for `DATABASE_URL`
- [x] Search for `PORT`
- [x] Search for auth-related environment keys
- [x] Search for `CORS` and `ORIGIN`
- [x] Search for `localhost`
- [x] List all server environment variables actually used
- [x] Compare actual usage against local server environment files
- [x] Confirm backend runtime depends on `DATABASE_URL`, `JWT_SECRET`, `JWT_EXPIRES_IN`, `NODE_ENV`, `PORT`, `CORS_ALLOWED_ORIGINS`, and auth rate-limit settings
- [x] Create `server/.env.example`
- [x] Create `server/.env.test.example`
- [x] Create root `/.env.example`
- [x] Identify extra Neon/Postgres variables in Vercel as likely non-blocking configuration residue

## 4) Remove localhost assumptions from production paths

### Frontend

- [x] Audit API base URL resolution
- [x] Confirm production requires `VITE_API_BASE_URL`
- [x] Confirm the frontend base must be the backend origin only, not an `/api` path
- [x] Confirm localhost fallback is development-only rather than production behavior
- [x] Confirm local development still works intentionally after env cleanup

### Backend

- [x] Audit config for localhost assumptions
- [x] Verify backend CORS configuration against the deployed frontend origin
- [x] Confirm production-safe origin handling
- [x] Verify database connection handling is production-safe
- [x] Confirm remaining localhost references are limited to acceptable local tooling, test tooling, or development defaults
- [x] Centralize auth rate-limit env validation in `env.ts`
- [x] Update rate-limit middleware to consume validated env config instead of reading directly from `process.env`

## 5) Re-validate production after cleanup

- [x] Re-run local backend validation:
  - typecheck
  - test suite
  - integration tests
  - build
- [x] Re-run local client validation:
  - test/build checks
- [x] Reload the deployed frontend after Sprint 1 merge to `main`
- [x] Re-check the browser Network tab for API targets after Sprint 1 merge to `main`
- [x] Re-check the backend health endpoint after Sprint 1 merge to `main`
- [x] Re-test a database-backed route after Sprint 1 merge to `main`
- [x] Confirm no production path depends on localhost after Sprint 1 merge to `main`
- [x] Confirm no CORS failures remain after Sprint 1 merge to `main`
- [x] Confirm frontend, backend, and database connectivity remains stable end-to-end after Sprint 1 merge to `main`

---

# Acceptance Criteria

- Production architecture is clearly documented
- Frontend communicates correctly with backend in production
- Backend communicates correctly with Neon in production
- No production behavior depends on localhost
- `.env.example` files are created and accurate
- Local validation passes before hosted post-merge verification

---

# Deliverables

- created `.env.example` files for root, client, server, and server test environments
- deployment notes for Vercel, Render, and Neon
- cleaned environment variable usage
- centralized backend env validation for auth rate-limit settings
- verified local connectivity/configuration baseline
- post-merge hosted verification checklist

---

# Notes

- Sprint 1 is about removing ambiguity and hidden fragility.
- No major hosting changes should happen in this sprint.
- This sprint creates the baseline for all later v3.0 hardening work.

---

### Deployment environment baseline

- Render is the active backend runtime environment and holds the backend-required variables.
- Vercel provides the frontend build-time API base URL through `VITE_API_BASE_URL`.
- Local client environment configuration was previously out of sync with actual client code expectations and was corrected during Sprint 1 hardening.
- Root Prisma tooling continues to use `DATABASE_URL` separately from frontend deployment configuration.
- Several Neon/Postgres variables in Vercel appear to be integration-managed rather than active frontend runtime requirements.

---

### Sprint 1 Environment Audit Findings

Sprint 1 identified several environment and deployment hardening points:

- The frontend uses `VITE_API_BASE_URL` as its API base variable.
- Older names such as `VITE_API_BASE` are stale and should not be used.
- The frontend base URL must be the backend origin only, not an `/api` path, because the client appends `/api` internally.
- The backend runtime is centered on `DATABASE_URL`, `JWT_SECRET`, `JWT_EXPIRES_IN`, `NODE_ENV`, `PORT`, `CORS_ALLOWED_ORIGINS`, and auth rate-limit settings.
- Root Prisma tooling uses `DATABASE_URL` separately from frontend deployment configuration.
- No `.env.example` files existed previously, so Sprint 1 included creating them from scratch.
- Auth rate-limit settings are now validated through centralized backend env parsing rather than being handled separately at runtime.

### Vercel managed environment variables

Several Neon/Postgres variables are still present in Vercel even though they do not appear to be used by the frontend application code.

These variables are **integration-managed** rather than manually created. Because they were provisioned through a connected Neon integration, they cannot be removed directly from the standard Environment Variables screen. Removing them would require disconnecting the project from the connected integration first.

For now, these variables are being treated as **non-blocking configuration residue** rather than active frontend requirements. The frontend’s relevant deployment variable remains `VITE_API_BASE_URL`, while backend and database runtime configuration are handled separately through Render and Prisma-backed tooling.

### Localhost audit result

Sprint 1 found several `localhost` references across local tooling and development assets. These references are not automatically deployment problems.

Confirmed acceptable local-only references include:

- local Postman environment configuration
- development and test tooling
- local CORS defaults in server env parsing
- client-side dev fallback behavior when running outside production

No evidence has been found so far that production runtime behavior depends on localhost. The more important configuration gap identified during this pass was that auth rate-limit environment variables were being used at runtime without centralized env validation, which has now been corrected as part of Sprint 1 hardening.
